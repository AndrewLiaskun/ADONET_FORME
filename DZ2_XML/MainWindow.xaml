<Window x:Class="DZ2_XML.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DZ2_XML"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="1" x:Name="dataGrid"></DataGrid>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="addButton" Content="Add" Width="100" Click="addButton_Click"/>
            <Button x:Name="updateButton" Content="Update" Width="100" Click="updateButton_Click"/>
            <Button x:Name="deleteButton" Content="Delete" Width="100" Click="deleteButton_Click"/>
            <Button x:Name="findButton" Content="Find" Width="100" Click="findButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
